package DataStruct.CH02_Array;

public class ArrayCalss {
    // 클래스는 서로 다른 여러 데이터 형을 자유로이 조합해서 만들 수 있는 자료구조 입니다.

    // 어느 그룹의 신체검사 데이터를 처리하는 것을 생각 (이름-키-시력) == (string-int-double)
    // 이런 여러 형의 요소를 조합하여 만든 자료구조가 클래스이다.

    /* 클래스 선언 예시1
    class XYZ {
    int x;
    long y;
    double z;
    }  = x는 인트형 필드, y는 롱 형 필드, z는 더블형 필드
    근데 이건 틀만 만들어 논거
    * */

    /*
    밑에가 인스턴스 화 시키는거

    XYZ a = new XYZ(); // 변수 선언 a, 인스턴스 생성, 연결짓기

    접근시에는
    a.x =
    a.y =
    a.z =

    * */

}
